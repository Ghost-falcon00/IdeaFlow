# 🚀 راهنمای دیپلوی امن روی VPS (کنار سرویس‌های دیگر)

این روش هیچ اختلالی در سرویس‌های فعلی (VPN، پنل‌ها و...) ایجاد نمی‌کند. سایت شما روی پورت‌های جداگانه (8080 و 8443) اجرا می‌شود.

## مرحله ۱: آماده‌سازی در سیستم خودتان
کدها را به گیت‌هاب بفرستید:
```bash
git add .
git commit -m "Ready for deploy"
git push origin main
```

---

## مرحله ۲: عملیات در VPS
به سرور SSH بزنید و مراحل زیر را خط به خط اجرا کنید.

### ۱. نصب پیش‌نیازها (اگر ندارید)
```bash
# نصب داکر و گیت
apt update
apt install -y git docker.io docker-compose
```

### ۲. دریافت پروژه
```bash
# کلون کردن پروژه (آدرس گیت خود را جایگزین کنید)
git clone https://github.com/USERNAME/REPO_NAME.git /opt/ideaflow
cd /opt/ideaflow
```

### ۳. تنظیم متغیرهای محیطی
یک فایل `.env` بسازید:
```bash
nano .env
```
اطلاعات زیر را در آن پیست کنید و ذخیره کنید (Ctrl+X, Y, Enter):
```ini
DEBUG=0
SECRET_KEY=یک_متن_تصادفی_و_طولانی_و_امن
ALLOWED_HOSTS=panel.ghostest.sbs,localhost,127.0.0.1

# Database (داکر خودکار این را می‌سازد، تغییر ندهید)
DB_NAME=ideaflow_db
DB_USER=ideaflow_user
DB_PASSWORD=secure_password_123
DB_HOST=db
DB_PORT=5432

# Email (برای ارسال ایمیل فراموشی رمز)
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password

# Ports (پورت‌های امن ما)
APP_PORT=8080
SSL_PORT=8443
```

### ۴. دریافت گواهی SSL (با ۱۰ ثانیه قطع پورت ۸۰)
این اسکریپت پورت ۸۰ سرور را چک می‌کند، سرویس مزاحم را موقت نگه می‌دارد، گواهی می‌گیرد و دوباره سرویس قبلی را روشن می‌کند.

```bash
chmod +x get_cert.sh
./get_cert.sh
```

### ۵. راه‌اندازی (اگر اسکریپت بالا خودکار انجام نداد)
```bash
chmod +x deploy.sh
./deploy.sh
```

---

## 🎉 تمام شد! سایت شما کجاست؟

- **نسخه امن (HTTPS):** `https://panel.ghostest.sbs:8443`
- **نسخه معمولی (HTTP):** `http://panel.ghostest.sbs:8080`

> **نکته مهم:** اگر فایروال (UFW) دارید، این پورت‌ها را باز کنید:
> `ufw allow 8080/tcp`
> `ufw allow 8443/tcp`
